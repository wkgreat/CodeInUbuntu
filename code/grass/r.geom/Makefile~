MODULE_TOPDIR = ../../SOURCE/grass_trunk/

PGM = r.geomorphon

LIBES = $(RASTERLIB) $(GISLIB) $(MATHLIB)
DEPENDENCIES = $(RASTERDEP) $(GISDEP)

include $(MODULE_TOPDIR)/include/Make/Module.make

default: cmd
